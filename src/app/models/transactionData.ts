export interface TransactionData {
  result: number,
  blockNumber: number,
  gasPrice: number,
  nonce: number,
  blockHash: string,
  transactionHash: number,
  blockMiningTime: number,
  fiboValue: number
}
