export interface Transaction {
  result: number,
  blockNumber: number,
  gasPrice: number,
  nonce: number,
  blockHash: string,
  transactionHash: number,
  blockMiningDuration: number,
  fiboValue: number,
  networkId: number
}
